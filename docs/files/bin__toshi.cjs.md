# `bin/toshi.cjs`

**Role**: `toshi` — launch the floating companion from anywhere, CONNECTED to this terminal. GPL-3.0. After `npm install -g .` (in the toshi repo), just type `toshi` in a
**LOC**: 311
**Imports**: node:path, node:child_process, node:os

## Used by
- `tools/verify-toshi.cjs`
